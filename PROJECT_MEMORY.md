# Project Memory

## Completed Stories
### US-005: Header, Boş Durum ve Hata Durumu Bileşenleri [done]
- Files: Created Header.tsx (top nav + side nav + bottom nav mobile), EmptyState.tsx (search_off empty state), ErrorState.tsx (error_outline error state). Components use onTabChange callback for active tab tracking. All UI text in Turkish.

